import { NamePreviewPipe } from './name-preview.pipe';

describe('NamePreviewPipe', () => {
  it('create an instance', () => {
    const pipe = new NamePreviewPipe();
    expect(pipe).toBeTruthy();
  });
});
